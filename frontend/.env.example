# commentToFix frontend configuration.
# Copy to .env.local for local overrides (Next.js loads it automatically).

# The FastAPI backend the app proxies /api/* to (and server components call).
#BACKEND_URL=http://localhost:4173
